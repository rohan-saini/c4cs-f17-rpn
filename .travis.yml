language: python
python:
  - "3.5"
# command to install dependencies
install:
  - pip install
  - pip install coveralls -r test_requirements
# command to run tests
script:
  - py.test coveralls/tests.py
after_success:
  - coveralls
